# Basic configuration for FaunaDB Local
storage:
  data_dir: /var/lib/faunadb

network:
  api_port: 8443
  graphql_port: 8084
  # bind_address: 0.0.0.0 is critical inside Docker
  bind_address: 0.0.0.0

# Optional: Disable clustering features for single-node local use
cluster:
  enabled: false
